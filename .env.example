# Sửa domain/email nếu dùng reverse proxy (Caddy) để có WSS + Let's Encrypt
DOMAIN=mqtt.example.com
LE_EMAIL=you@example.com

# Cổng expose ra host (cứ để mặc định trừ khi bạn cần đổi)
MQTT_PORT=1883
MQTT_WS_PORT=9001
